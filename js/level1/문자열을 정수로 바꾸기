
//풀이
//array.split 을 통해 "-"가 없으면 바로 변환, 있으면 [1]을 변환 후 -1 곱하기
function solution(s) {
    var answer = s.split("-");
    return answer.length <= 1?Number(answer[0]):Number(answer[1]) * -1;
}

// 다른 풀이
// s/1을 실행 시 s(ex: "-1234")가 int형으로 변환
function solution(s) {
    return s/1;
}
